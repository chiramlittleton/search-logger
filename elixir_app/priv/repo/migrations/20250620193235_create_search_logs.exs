defmodule ElixirApp.Repo.Migrations.CreateSearchLogs do
  use Ecto.Migration

  def change do
#    create table(:search_logs) do
#      add :user_id, :uuid
#      add :session_id, :text
#      add :keyword, :text, null: false
#      add :created_at, :utc_datetime, default: fragment("NOW()")
#    end
#
#    create index(:search_logs, [:session_id])
  end
end
